{% include 'default/_form.html.twig' with {
    page_title: 'title.ingredient_delete',
    page_title_param: {'%id%': ingredient.id},
    attr: {
        method: 'post',
        action: url('ingredients_delete', {'id': ingredient.id}),
    },
    back_to_list_path: 'ingredients_index',
    item_name: ingredient.name,
} %}