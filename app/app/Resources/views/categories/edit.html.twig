{% include 'default/_form.html.twig' with {
    page_title: 'title.category_edit',
    page_title_param: {'%id%': category.id},
    attr: {
        method: 'post',
        action: url('categories_edit', {'id': category.id}),
    },
    back_to_list_path: 'categories_index',
} %}