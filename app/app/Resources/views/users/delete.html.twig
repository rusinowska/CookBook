{% include 'default/_form.html.twig' with {
    page_title: 'title.user_delete',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'post',
        action: url('users_delete', {'id': user.id}),
    },
    back_to_list_path: 'users_index',
    item_name: user.username,
} %}
