{% include 'default/_form.html.twig' with {
    page_title: 'title.recipe_filter',
    attr: {
        method: 'post',
        action: url('recipes_filter', {'title': recipe.title})
    },
    back_to_list_path: 'recipes_index',
} %}